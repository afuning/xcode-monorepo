export default function HomePage() {
  return (
    <section>
      <h1>Welcome to the monorepo</h1>
      <p>
        This is the Next.js app. The Flutter app lives in the
        <code> apps/mobile</code> directory.
      </p>
      <p>
        This app will fetch captured Weibo data from the backend and render the
        results as charts for reporting and trend analysis.
      </p>
    </section>
  );
}
